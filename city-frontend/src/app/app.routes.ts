import { Routes } from '@angular/router';
import { CitiesComponent } from './cities/cities.component';

// export const routes: Routes = [
//     { path: 'cities/:viewMode', component: CitiesComponent },
//     { path: '', redirectTo: '/cities/card', pathMatch: 'full' }
// ];

export const routes: Routes = []
